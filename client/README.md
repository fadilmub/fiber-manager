# Fiber Manager - React Client

This is a minimal React (Vite) scaffold to migrate `index.html` into a React app.

Quick start:

```bash
cd client
npm install
npm run dev
```

The app expects the PHP API to be available at `/fiber-manager/api` (same host). Leaflet is loaded from CDN in `index.html`.
